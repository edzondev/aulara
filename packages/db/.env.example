# Copy to .env and replace with the real connection string.
# This file is used by drizzle-kit and database scripts. Never commit .env files.
DATABASE_URL=postgresql://user:password@host.db.provider.com/neondb?sslmode=require
